title : Ruby basic POC
plan:
  - title: Ruby basic POC with test-unit
    statement: markdowns/part1.md
projects:
  ruby:
   root: root
   buildCommand: "cd /project/target && chmod +x prebuild.sh && ./prebuild.sh"
   runner:
    name: ruby
    version: latest